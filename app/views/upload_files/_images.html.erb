<%= simple_form_for UploadFile.new, :html => { :class => 'form-horizontal', :id => "new_image" } do |f| %>
  <%= f.input :image, multiple: true, name: "upload_file[image]", :type => :file, :label => "请选择图片" %>
<% end %>